# Local dev: leave unset — Vite's dev-server proxy handles /api.
# Vercel production: set this in the Vercel project's Environment Variables
# to your Render backend URL, e.g.:
# VITE_API_BASE_URL=https://your-backend.onrender.com/api
